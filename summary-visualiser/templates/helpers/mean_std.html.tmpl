{{/*
Render mean and standard deviation values.
Fields:

* `mean` (scalar): The mean value.
* `std` (scalar): The standard deviation value.
* `unit` (string, optional): The unit to display alongside the values.
*/}}
<span class="mean-std">
    {{ template "mean" . }}
    {{ template "std" . }}
</span>
