{{/*
Render a min ≤ (mean ± std) ≤ max summary.
Fields:

* `min` (scalar): The minimum value.
* `mean` (scalar): The mean value.
* `std` (scalar): The standard deviation value.
* `max` (scalar): The maximum value.
* `unit` (string, optional): The unit of measurement to display alongside all values.

Corresponding Rust type: `holochain_metrics::GaugeStats`
*/}}
<span class="min-max-mean-std">
    <span class="min-max-mean-std-min">
        {{ template "scalar" (dict
            "label" "min "
            "value" .min
            "unit" (index . "unit")
        ) }}
    </span>
    {{ $min_mean_symbol := test.Ternary "lt" "le" (lt .min .mean) }}
    <span class="divider divider-{{ $min_mean_symbol }}">&{{ $min_mean_symbol }};</span>
    {{ template "mean_std" . }}
    {{ $mean_max_symbol := test.Ternary "lt" "le" (lt .mean .max) }}
    <span class="divider divider-{{ $mean_max_symbol }}">&{{ $mean_max_symbol }};</span>
    <span class="min-max-mean-std-max">
        {{ template "scalar" (dict
            "label" " max"
            "value" .max
            "unit" (index . "unit")
        ) }}
    </span>
</span>