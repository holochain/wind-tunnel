[package]
name = "timed"
version = "0.1.0"
edition = "2021"
description = "A Holochain coordinator zome for timing data visiblity"

[lib]
name = "timed_coordinator"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = { workspace = true }
timed_integrity = { workspace = true }

[lints]
workspace = true
