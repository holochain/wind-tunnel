[package]
name = "signal"
version = "0.1.0"
edition.workspace = true
description = "A Holochain coordinator zome for emiting signals"
publish = false

[lib]
name = "signal_coordinator"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
hdk = { workspace = true }

[lints]
workspace = true
