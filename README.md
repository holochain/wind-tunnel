# Wind Tunnel Github Pages Site

## Development

To run the site locally:

1. Enter the nix shell with `nix develop`
2. Serve the jekyll site with `jekyll serve`

Note that when the site is deployed, Github's default jekyll theme 'minima' will be applied.